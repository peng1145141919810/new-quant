# Runtime Docs Overview

This folder contains runtime-side documentation for the active Python tree under `src\ashare`.

## Current Reality
- The active runtime root is `src\ashare`, not the deleted repacked directory tree.
- The canonical top-level entry remains `..\..\launch_canonical.py`.
- The direct runtime dispatcher remains `..\..\main_research_runner.py`.
- The trade clock entry remains `..\..\trade_clock_service.py`.

## Read Order
1. `..\..\CODEX_DEV_STABLE.md`
2. `..\..\docs\SYSTEM_OPERATOR_DAILY_LOOP_GUIDE_CN.md`
3. Topic-specific docs in this folder only when needed

## Key Topics Still Covered Here
- PyCharm and local runtime setup notes
- runtime patch notes
- bridge integration notes
- research brain packaging history

If anything in this folder disagrees with `CODEX_DEV_STABLE.md`, the stable snapshot wins.
